// import axios from "axios";
// import { UserProfile } from "../models/user";

// const baseUrl = import.meta.env.VITE_API_URL || "";

// export const login = async (uid: string): Promise<UserProfile> => {
//   const user = await axios.get(baseUrl + "auth/login", { uid });
//   return user.data;
// };
